{
  "devDependencies": {
    "terser": "^5.43.1",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node scripts/build.js",
    "watch": "tsc -w"
  }
}
